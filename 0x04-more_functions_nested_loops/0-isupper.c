#include "main.h"

/**
 * _isupper - this will checheck for uppercase characters
 * @c: this is the character to be checked
 * Return: 1 if c is uppercase, otherwise 0
 */

int_isupper(int c)

{
	return(c >= 'A' && c <= 'Z');
}
